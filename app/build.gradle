apply plugin: 'com.android.application'
android {
    signingConfigs {
        config {
            keyAlias 'ivixor_keys'
            keyPassword 'p4r4d1s3'
            storeFile file('D:/Documents/android.jks')
            storePassword 'p4r4d1s3'
        }
    }
    compileSdkVersion 19
    buildToolsVersion "20.0.0"
    defaultConfig {
        applicationId "com.example.android.senddirect"
        minSdkVersion 16
        targetSdkVersion 16
    }
    buildTypes {
        release {
            signingConfig signingConfigs.config
            applicationVariants.all { variant ->
                variant.outputs.each { output ->
                    output.outputFile = new File(output.outputFile.parent,
                            output.outputFile.name.replace("app-release.apk", "sendirect.apk")
                    )
                }
            }
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_7
        targetCompatibility JavaVersion.VERSION_1_7
    }
}

dependencies {
    compile project(':joda-time-2.6')
    compile project(':achartengine-1.1.0')
}